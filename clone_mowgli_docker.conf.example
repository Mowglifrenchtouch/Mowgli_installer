# clone_mowgli_docker.conf.example
# =================================
# Exemple de configuration pour le clonage du dépôt mowgli-docker
# ➜ Copiez ce fichier sous le nom clone_mowgli_docker.conf
# ➜ Puis personnalisez les variables ci-dessous selon vos besoins.

# 🔗 URL du dépôt Git à cloner
# Si vous utilisez un fork personnel, modifiez cette URL.
REPO_URL="https://github.com/cedbossneo/mowgli-docker.git"

# 🌿 Branche Git à utiliser (par défaut : main)
BRANCH="cedbossneo"

# 📁 Répertoire de destination
# Ce répertoire sera remplacé s’il existe déjà.
TARGET_DIR="${HOME}/mowgli-docker"
